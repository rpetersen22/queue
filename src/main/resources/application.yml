spring:
  application:
    name: queue
  datasource:
    url: jdbc:h2:~/mydb
    username: test
    password: placeholder
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update